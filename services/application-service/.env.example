# Database — connect to the shared MySQL container
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=yourpassword
DB_NAME=linkedin_simulation

# Service port
PORT=3003

# Kafka — change to kafka:9092 when running inside Docker Compose
KAFKA_BROKER=localhost:9092
KAFKA_CLIENT_ID=application-service